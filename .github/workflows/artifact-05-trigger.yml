name: NEXUS Artifact 05 Trigger

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    name: Artifact 05 trigger is superseded
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Artifact 05 now triggers directly from NEXUS Verification via artifact-05-final.yml.'
