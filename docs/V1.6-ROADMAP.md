# NEXUS v1.6 Roadmap — Exact Promotion Execution & Drift Containment

Status: **COMPLETE / SEALED AS v1.6.0**

NEXUS v1.6 follows the sealed v1.5.0 promotion-safety boundary. It advances only the execution claim that v1.5 intentionally refused to make: an exact provider-side source binding for the existing primary production service, followed by a new successful deployment while preserving the durable state boundary.

## Constitutional invariant

`A_out <= A_in`

Production mutation is an execution act. Verification can establish eligibility and evidence can establish observation, but neither independently grants authority to widen the production boundary.

## Exact promotion target

- Historical release target: `v1.4.0`
- Exact target commit: `dcf49302e8a8c4c935de3aad0585b19132bad2cd`
- Existing production service: `nexus-stage-d`
- Existing production environment: `production`
- Existing durable mount: `/data`

## Verified gates

### G1 — Historical release integrity — PASS
- `v1.4.0` remains fixed to `dcf49302e8a8c4c935de3aad0585b19132bad2cd`.
- v1.5.0 remains a historical record of the earlier fail-closed state and is not rewritten.

### G2 — Exact provider-side source binding — PASS
- Railway service configuration contains an explicit immutable `source.commitSha` equal to `dcf49302e8a8c4c935de3aad0585b19132bad2cd`.
- The production source is therefore no longer represented solely by a moving `main` branch.

### G3 — Promotion execution — PASS
- The staged exact-SHA configuration was committed through the provider.
- Post-change Railway deployment: `bc72bf4d-7e0b-4f19-9a74-25ca2f76d917`.
- Provider deployment status: `SUCCESS`.

### G4 — Durable-state continuity — PASS
- The existing volume remained attached.
- Mount path remained `/data`.
- No volume deletion, replacement, or paid storage expansion was performed.

### G5 — Network and runtime continuity — PASS
- Service domain remained `nexus-stage-d-production.up.railway.app` on target port `3000`.
- Runtime startup evidence reports `data_dir=/data` and authentication configured.

### G6 — Configuration non-expansion — PASS
- Existing variable names remained `NEXUS_API_TOKEN`, `NEXUS_BIND_ADDR`, `NEXUS_DATA_DIR`, and `PORT`.
- No secret values are recorded in repository evidence.
- No new service, privilege surface, authorization rule, or paid resource was introduced by the promotion.

### G7 — Provider metadata distinction — EXPLICIT
- Railway deployment metadata for the post-change deployment still reports `meta.commitHash=81f493b1fe3bc964bba51df905c2d448af13bdf4` and `branch=main` as trigger/context metadata.
- NEXUS does **not** treat that field as proof of the selected source after the provider configuration was explicitly pinned.
- The exact-source claim for this stage is bounded to the active provider configuration `source.commitSha`, the subsequent successful deployment, and the preserved durable/runtime configuration.
- v1.6 does not claim a stronger property such as independent binary reproducibility from Railway's deployment metadata alone.

### G8 — Operational economy — PASS
- No paid plan upgrade was authorized or used for this stage.
- Existing service, domain, variables, and durable volume were reused.

### G9 — Exact-candidate release gate — PASS
- Exact release-intent candidate: `60fc683808d4bed11b051546d7215aff50df0ade`.
- `NEXUS Verification` run `34881242396`: `SUCCESS`.
- GitHub release `v1.6.0`: `PUBLISHED` and targeted to the exact verified candidate.

## Final state

NEXUS v1.6.0 is sealed at the stated exact-promotion boundary. The existing production service is pinned to the exact sealed v1.4.0 source commit and a new provider deployment completed successfully while `/data`, authentication configuration, and the existing service boundary were preserved.

The provider trigger/context metadata discrepancy remains part of the evidence record rather than being normalized away. Accordingly, the seal does not claim formal proof that trigger metadata is identical to selected-source metadata, independent binary reproducibility from Railway metadata alone, universal availability, universal security, or formal epistemic correctness.

The `v1.6.0` tag is the immutable historical boundary. Later documentation commits on `main` do not retroactively alter that release.

See `evidence/v1.6/railway-promotion-evidence.json` and `docs/RELEASE-v1.6.0.md` for the release evidence boundary.
