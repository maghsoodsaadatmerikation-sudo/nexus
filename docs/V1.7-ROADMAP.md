# NEXUS v1.7 Roadmap — Runtime Provenance & Source-Tree Identity

Status: **COMPLETE / SEALED AS v1.7.0**

NEXUS v1.7 follows the sealed v1.6.0 exact-promotion boundary. v1.6 established an explicit provider-side source pin and successful production deployment, but deliberately did not claim that Railway trigger metadata independently proved the selected source or that the running binary was independently reproducible from provider metadata alone.

v1.7 closes a narrower, testable gap: the executable image embeds a deterministic digest of the source/build-input tree, exposes that digest as explicitly non-authoritative runtime metadata, and compares the observed production value against independently computed repository evidence.

## Constitutional invariant

`A_out <= A_in`

Runtime identity metadata may report provenance evidence. It cannot authorize requests, create HumanJudgment, mutate policy, widen execution authority, or substitute for a release decision.

## Source-tree identity boundary

The canonical digest is computed by `scripts/source-tree-digest.sh` over the build inputs that determine the executable/product surface:

- root `Cargo.toml` and `Cargo.lock`;
- `Dockerfile`;
- constitutional-core `src/`;
- `artifact-05/` source and lockfile;
- `web/` assets embedded by the gateway;
- the digest script itself.

Build outputs under `target/` are excluded.

The Docker build computes this digest before compiling and embeds it through `NEXUS_SOURCE_TREE_SHA256`. The running service exposes:

`GET /v1/runtime-identity`

with schema `nexus.runtime-identity.v1`, the embedded source-tree digest, and the explicit authority marker `non_authoritative_build_metadata`.

## Gates

### G1 — Historical integrity — PASS
- `v1.6.0` remained immutable.
- v1.7 added evidence rather than rewriting prior release claims.

### G2 — Deterministic source-tree function — PASS
- A single canonical script computes the digest.
- The Docker build consumes that same script.
- Missing build inputs fail closed.

### G3 — Embedded runtime identity — PASS
- The executable exposes the embedded digest through a read-only endpoint.
- Runtime identity is labelled `non_authoritative_build_metadata`.
- No secret value, bearer token, user data, workspace data, or policy decision is exposed by the endpoint.

### G4 — Independent CI reconciliation — PASS
- Exact runtime target: `3c0977f4c7fc9fb5e9a78bdb1eeb581cb3cb4959`.
- `NEXUS Verification` run `34884330885`: SUCCESS.
- `NEXUS v1.7 Runtime Identity` run `34884505985`: SUCCESS.
- Canonical source-tree SHA-256: `d66021fc1e8c276df2c673d26b05f2de58ef4c81589261cb41aefa1ad047f1c7`.
- Independent expected/runtime digest equality: PASS.

### G5 — Production reconciliation — PASS
- Railway production service `nexus-stage-d` is pinned to exact commit `3c0977f4c7fc9fb5e9a78bdb1eeb581cb3cb4959`.
- Provider deployment `b0e78cf8-af51-4092-9bf6-99e1ac5cc13b`: SUCCESS.
- Existing durable volume remained mounted at `/data`.
- Public production reconciliation run `34886934100`: SUCCESS.
- `GET /v1/runtime-identity`: HTTP `200`.
- Observed schema: `nexus.runtime-identity.v1`.
- Observed authority marker: `non_authoritative_build_metadata`.
- Observed production digest exactly matched the independently computed canonical digest.
- No paid upgrade, new production service, new production volume, secret disclosure, or authority expansion was required or authorized.

### G6 — Release gate — PASS
- Production evidence is recorded in `evidence/v1.7/production-runtime-evidence.json`.
- Exact release-intent candidate: `9847b293f3b37a574568ba4e450797b312a44984`.
- Exact-candidate `NEXUS Verification` run `34887158522`: SUCCESS.
- `NEXUS v1.7 Release Gate` run `34887347369`: SUCCESS.
- GitHub Release `v1.7.0`: PUBLISHED, targeting exact candidate `9847b293f3b37a574568ba4e450797b312a44984`.

## Claim boundary

v1.7 may claim that the observed production executable reports a source-tree identity exactly matching the independently computed digest for the recorded target build inputs, under the stated Railway deployment and durable-state boundary.

It may not claim bit-for-bit reproducible binaries across arbitrary builders, formal verification of Railway, universal security, universal availability, or formal proof of epistemic correctness.

## Final state

v1.7.0 is sealed at the runtime-provenance boundary. Historical release identity, exact provider source binding, independent repository digest computation, observed production runtime identity, durable-state continuity, and release authorization remain distinct evidence layers.

Later duplicate release-gate invocations are expected to fail closed because `v1.7.0` already exists; such duplicate refusal does not invalidate the successful seal.
