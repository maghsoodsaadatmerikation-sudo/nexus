# NEXUS v1.5 Roadmap — Promotion Safety & Rollback Readiness

Status: **COMPLETE / SEALED AS v1.5.0**

NEXUS v1.5 follows the sealed v1.4.0 deployment-truth boundary. It does not rewrite historical tags, release records, or provider observations.

## Constitutional invariant

`A_out <= A_in`

Production promotion is an execution act, not an inference. A verified release may be eligible for promotion, but verification alone must never be treated as authorization to mutate production.

## Goal

Define and enforce a fail-closed production-promotion contract that prevents NEXUS from claiming or performing a production cutover unless the exact target commit, durable-state boundary, rollback anchor, and provider-side post-cutover evidence are all explicit.

## Gates

### G1 — Historical release integrity — PASS
- `v1.4.0` remains fixed to `dcf49302e8a8c4c935de3aad0585b19132bad2cd`.
- Historical release evidence remains additive-only.

### G2 — Promotion target identity — PASS
- The only eligible promotion target for this stage is the exact sealed `v1.4.0` commit.
- The exact sealed `v1.4.0` commit is required; a moving `main` branch is not an acceptable substitute for immutable identity.

### G3 — Pre-cutover durable-state safety — PASS
- The observed production persistence boundary remains `/data`.
- No destructive production-data test was performed.
- The observed live deployment remained the rollback anchor.

### G4 — Exact-SHA execution requirement — PASS
- Exact immutable identity is mandatory for production promotion.
- Connected provider tooling did not expose a verified exact-commit source pin for the existing primary service.
- Therefore promotion must stop rather than approximate through a moving branch.

### G5 — Post-cutover evidence requirement — PASS
- The contract requires new provider-side `SUCCESS` evidence, exact promoted identity, `/data`, and no authority expansion after any future cutover.
- No false post-cutover evidence was created because no cutover occurred.

### G6 — Rollback readiness — PASS
- Pre-cutover live commit: `480f8771c47e41d4d19b22ed4360bd32c4d2f70a`.
- Pre-cutover deployment: `1f63e3b9-768f-441e-a524-b061394b922e`.
- Automatic rollback is not authorized by verification CI.

### G7 — Operational economy — PASS
- No paid resource upgrade was required or authorized.
- No new service or volume was created for this stage.

### G8 — v1.5 release gate — PASS
- Exact candidate commit: `84d1c283494e1203e63d6a892351938dfc24a5ed`.
- NEXUS Verification run: `34879097015` — SUCCESS.
- GitHub Release: `v1.5.0` — PUBLISHED.

## Final state

v1.5.0 is sealed as a promotion-safety and rollback-readiness protocol. Production promotion remains intentionally **NOT PERFORMED** because exact provider-side source binding was not established by the connected tooling.

That blocked mutation is the intended fail-closed behavior, not an incomplete verification result. Release readiness, promotion authorization, promotion execution, and observed production state remain distinct.

See `docs/RELEASE-v1.5.0.md` for the sealed release record.
